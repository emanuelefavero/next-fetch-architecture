import type { StaggerSpeed } from '@/lib/animations/fade-in-up'

/**
 * Configuration constants for the users feature
 */
export const USERS_PER_PAGE = 12

/**
 * Default stagger speed for user list animations
 */
export const DEFAULT_STAGGER_SPEED: StaggerSpeed = 'medium' as const
